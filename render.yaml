service: #A docker web service
  - type: web
    name: capstone-sales
    runtime: python
    plan: free
    autodeploy: false
    buildCommand: ./build.sh
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT